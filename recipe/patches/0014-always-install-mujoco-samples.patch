diff --git a/sample/CMakeLists.txt b/sample/CMakeLists.txt
index defcf85..398749d 100644
--- a/sample/CMakeLists.txt
+++ b/sample/CMakeLists.txt
@@ -42,7 +42,7 @@ set(_INSTALL_SAMPLES ON)
 if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
   set(SAMPLE_STANDALONE ON)
   # If standalone, do not install the samples.
-  set(_INSTALL_SAMPLES OFF)
+  #set(_INSTALL_SAMPLES OFF)
 endif()
 
 list(APPEND CMAKE_MODULE_PATH "${PROJECT_SOURCE_DIR}/cmake")
@@ -170,8 +170,6 @@ endif()
 
 if(_INSTALL_SAMPLES)
 
-  include(TargetAddRpath)
-
   # Add support to RPATH for the samples.
   set(MUJOCO_INSTALL_SAMPLES
       basic
@@ -184,16 +182,6 @@ if(_INSTALL_SAMPLES)
     list(APPEND MUJOCO_INSTALL_SAMPLES render)
   endif()
 
-  target_add_rpath(
-    TARGETS
-    ${MUJOCO_INSTALL_SAMPLES}
-    INSTALL_DIRECTORY
-    "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_BINDIR}"
-    LIB_DIRS
-    "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}"
-    DEPENDS
-    MUJOCO_ENABLE_RPATH
-  )
 
   install(
     TARGETS ${MUJOCO_INSTALL_SAMPLES}
